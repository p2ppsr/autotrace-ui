import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
    heading: {
        paddingTop: '15vh'
    },
    text: {
        marginTop: '0.5vh'
    },
}));

export default useStyles;